find min iterative run time:

for(int i = 1; i<arr.length; i++) Executed n times

Running time: O(n)

find min recursive run time:

if(arr[min] > arr[index-1]){ Executed n times
			return index-1;
		} else {
			return min;

Running time: O(n)